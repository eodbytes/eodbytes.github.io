// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://eodbytes.github.io',
    // If deploying to https://<user>.github.io/<repo>/, uncomment and set base:
    // base: '/eodbytes',
});
